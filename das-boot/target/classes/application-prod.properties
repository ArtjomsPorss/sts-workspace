#add to run configurations-Vm arguments and run as that configuration to use this application.properties
#-Dspring.profiles.active=test

#standard port for production environment
server.port=80

#Data source connection for non-embedded database
spring.datasource.url=jdbc:h2:file:~/dasboot
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver